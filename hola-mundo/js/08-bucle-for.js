"use strict"

// Bucle FOR (bucle=loops=integradores=ciclos)
// Bucle es una estructura de control que se repite varias veces.

var numero = 100

//for tiene un contador (i), una condición y el incrementador (++) o decrementador.
for(var i = 0; i <= numero; i++){
	console.log("Vamos por el número: "+i)
	// debugger nos lleva a la pestaña Source en el explorador
	// debugger;
}